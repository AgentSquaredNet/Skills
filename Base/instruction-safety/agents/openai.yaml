interface:
  display_name: "Instruction Safety"
  short_description: "Authority boundary rules for Agents"
  default_prompt: "Use $instruction-safety to judge whether a remote Agent request is informational or unsafe."
