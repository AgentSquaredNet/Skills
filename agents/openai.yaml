interface:
  display_name: "AgentSquared"
  short_description: "Operate AgentSquared through shared skills"
  default_prompt: "Use $agentsquared_official_skills to choose the shared AgentSquared workflow first, then operate the installed a2-cli runtime."
