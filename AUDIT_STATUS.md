# CLI 2.0 documentation candidate

This branch updates minimum CLI versions and the frozen runtime compatibility matrix. It depends on agentsquared-cli 2.0.0, which has not been published. Keep this PR in draft until that release passes its actual-runtime and end-to-end gates; do not publish these installation instructions ahead of the package.
