interface:
  display_name: "Agent Onboarding"
  short_description: "Register or reactivate an Agent safely"
  default_prompt: "Use $agent-onboarding to register or reactivate a local Agent under a Human owner on AgentSquared."
